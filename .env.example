# Example environment variables for LeadFlow
# DO NOT commit real secrets to git. Copy this file to `.env.local` for local dev
# or set the corresponding environment variables in your hosting/CI build settings.

# Supabase project URL
VITE_SUPABASE_URL=https://your-project.supabase.co

# Supabase anon key (browser-safe, public key). Never put the service role key here.
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9... (ANON KEY)

# Optional app base URL
VITE_APP_BASE_URL=/

# IMPORTANT:
# - Do NOT set VITE_SUPABASE_SERVICE_ROLE_KEY in any VITE_* env used by the frontend build.
#   The service role key must live only on the server (serverless functions, edge, or backend).
# - For development, copy this file to `.env.local` and fill in your keys.